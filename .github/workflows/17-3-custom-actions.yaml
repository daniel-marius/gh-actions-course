name: 17 - 3 - Custom Actions - Docker

on:
    workflow_dispatch:
        inputs:
            url:
                type: choice
                options:
                    - 'https://www.google.com'
                    - 'https://invalid-abcd.com'
                    - 'invalid-abcd'
            max_trials:
                description: Maximum numbers of trials until action fails
                default: '10'
                required: false
            delay:
                description: Delay in seconds betwwen trials
                default: '5'
                required: false
            debug:
                type: boolean
                default: false

jobs:
    ping-url:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Ping URL
              id: ping-url
              uses: ./.github/actions/docker-ping-url
              with:
                url: ${{ inputs.url }}
                max_trials: ${{ inputs.max_trials }}
                delay: ${{ inputs.delay }}
            - name: Print output from ping url
              run: |
                echo "URL reachable: ${{ steps.ping-url.outputs.url-reachable }}"